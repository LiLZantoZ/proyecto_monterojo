# Bloquea cualquier acceso web directo a esta carpeta (contiene credenciales de BD y lógica interna)
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
